# Синтаксис и команды MathLang

## 1. Присваивание

**Синтаксис:** `имя_переменной = выражение`

**Действие:** Вычисляет выражение и сохраняет результат в переменной. Если переменная не существовала, она создаётся.

**Пример:**
```math
radius = 10
pi = 3.1415926
area = pi * radius^2
